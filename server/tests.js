import Repos from "../client/src/containers/Repos";

// BACKEND
// check for deadlock, data corruption, data loss, etc
// test "DB" result after feeding a bookmarkedRepo and a new search result from the API

// send repos to "DB" using the githubresExample and check if the data was stored as expected

// test business logic like search for Language Python should yield Python language results
// FRONTEND
